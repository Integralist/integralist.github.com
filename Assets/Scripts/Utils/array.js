define(function(require){

	return {
		filter: require('Utils/Array/filter'),
		forEach: require('Utils/Array/forEach'),
		indexOf: require('Utils/Array/indexOf'),
		unique: require('Utils/Array/unique')
	};

});